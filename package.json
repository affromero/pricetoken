{
  "name": "pricetoken-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@pricetoken/web",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build",
    "setup-dns": "doppler run -- npx tsx scripts/setup-dns.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "eslint-plugin-next": "^0.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "prettier": "^3.4.0"
  }
}
